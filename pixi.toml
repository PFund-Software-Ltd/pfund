[workspace]
authors = ["softwareentrepreneer <softwareentrepreneer+pfund@gmail.com>"]
channels = ["conda-forge"]
name = "pfund"
# macos = min version of macOS required by torch>=2.12.0
platforms = [{ platform = "osx-arm64", macos = "14.0" }, "linux-64"]
# NOTE: should match with setup-pixi in ci.yml
requires-pixi = ">=0.81.0,<0.82.0"
# FIXME: set it back to "14 days" when the package is stable
exclude-newer = "0d"

[pypi-exclude-newer]
afterpython = "0d"
pfeed = "0d"
pfolio = "0d"
pfund-kit = "0d"
pfund-plot = "0d"
mtflow = "0d"

[tasks]

[tasks.test-all]
depends-on = [
  { task = "test", environment = "py312" },
  { task = "test", environment = "py313" },
  { task = "test", environment = "py314" },
  # { task = "test", environment = "py315" },
]

[environments]
default = ["py312", "dev", "optional"]
py312 = ["py312", "test", "optional"]
py313 = ["py313", "test", "optional"]
py314 = ["py314", "test", "optional"]
# py315 = ["py315", "test", "optional"]

[dependencies]

[pypi-dependencies]
pfund-kit = ">=0.1.20"
pfeed = ">=0.0.20"
apscheduler = ">=3.11.3"
httpx2 = { version = ">=2.13.0", extras = ["http2"] }
websockets = ">=16.1.0"
numba = ">=0.67.0"

[feature.optional.pypi-dependencies]
pfeed = { version = ">=0.0.20", extras = ["yfinance"] }
pfolio = ">=0.0.4"
pfund-plot = {version = ">=0.0.5", extras = ["marimo"]}
# TODO: enable when ready
# mtflow = ">=0.0.1.dev2"
pfund-ibapi = "==10.50.2"
alpaca-py = ">=0.44.0"
hyperliquid-python-sdk = ">=0.24.0"
joblib = ">=1.6.0"
scikit-learn = ">=1.9.1"
torch = ">=2.14.0"
safetensors = ">=0.8.0"
jax = ">=0.11.2"
ta-lib = ">=0.8.0"

[feature.dev.pypi-dependencies]
pfund = { path = ".", editable = true }
# TEMP
pfeed = { path = "../pfeed", editable = true, extras = ["yfinance"] }
# pfund-plot = { path = "../pfund-plot", editable = true, extras = ["marimo"] }
pfund-kit = { path = "../pfund-kit", editable = true }
# pfolio = { path = "../pfolio", editable = true }
mtflow = { path = "../mtflow", editable = true }
afterpython = ">=0.3.19"
basedpyright = ">=1.40.1"
memray = ">=1.19.3"
pandas-stubs = ">=3.0.3.260530"
pybit = ">=5.17.0"
lightning = ">=2.6.6"
flax = ">=0.12.9"
equinox = ">=0.13.8"
optax = ">=0.2.8"
transformers = ">=5.17.0"

[feature.dev.dependencies]
graphviz = ">=14.1.2"
python-graphviz = ">=0.21"

[feature.test.pypi-dependencies]
pfund = { path = ".", editable = true }
pytest = ">=9.1.1"
pytest-cov = ">=7.1.0"
pytest-mock = ">=3.15.1"
pytest-xdist = ">=3.8.0"
pytest-asyncio = ">=1.4.0"
pytest-recording = ">=0.13.4"
# testcontainers = ">=4.14.2"
[feature.test.tasks]
test = "pytest"
test-new = "pytest --record-mode=all"  # record new interactions, useful to update recordings in case data formats returned have changed
test-ci = "pytest --record-mode=none"  # fail a test if no recording is found

[feature.py312.dependencies]
python = "3.12.*"

[feature.py313.dependencies]
python = "3.13.*"

[feature.py314.dependencies]
python = "3.14.*"

# [feature.py315.dependencies]
# python = "3.15.*"
